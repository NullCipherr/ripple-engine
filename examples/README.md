# Exemplos Oficiais

## Conteúdo

- `vanilla/`: integração completa com `createVanillaRippleAdapter`.
- `react/`: integração orientada a ciclo de vida com `createReactRippleAdapter`.

## Objetivo

Demonstrar setup e teardown corretos para consumidores da `R.I.P.P.L.E`.
